# photo-group

A web app where users sign in with Google, generate a QR code that others scan to submit photos stored in Google Drive folders organized by date.

## Requirements

- [Bun](https://bun.sh) >= 1.3

## Development

```bash
bun install
bun dev
```

## Env vars

Copy `.env.example` to `.env.local` and fill in the values.
